# Project Prompts

This directory is the recommended location for project-specific prompts referenced by AEEF attribution fields (`AI-Prompt-Ref`).

Recommended convention:

- `prompts/<domain>/<use-case>/<name>.vX.Y.Z.md`
- include metadata header with owner, model assumptions, and test evidence
