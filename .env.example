# Route feedback popup submissions to your webhook endpoint.
# Default behavior (if unset) uses FormSubmit to send to info@codemeld.io.
FEEDBACK_ENDPOINT="https://your-webhook-url"

# Optional contact email metadata (used in site config/custom fields).
FEEDBACK_EMAIL="you@example.com"
